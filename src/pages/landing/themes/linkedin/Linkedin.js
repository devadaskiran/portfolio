import React from 'react'

import styles from './Linkedin.module.scss';

const Default = () => {

    return (
        <div className={`d-flex w-100 align-items-center justify-content-center ${styles.wrapper}`}>
            Coming Soon ! :)
        </div>
    );
};

export default Default
