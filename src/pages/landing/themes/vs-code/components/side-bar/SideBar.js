import React, { useState, useEffect } from 'react'

import styles from './SideBar.module.scss'

const SideBar = () => {
    return (
        <div className={`d-flex h-100 ${styles.wrapper}`}>
            {}
        </div>
    );
};

export default SideBar
